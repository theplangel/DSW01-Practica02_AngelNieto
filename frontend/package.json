{
  "scripts": {
    "start": "http-server -p 8080 ./",
    "cy:run": "cypress run --headless --config baseUrl=http://localhost:8080"
  },
  "devDependencies": {
    "cypress": "^15.14.1",
    "http-server": "^14.1.1"
  }
}
